import pytest

from src.Figure import Figure

class Test:

    def test_add_figure(self):
        if not isinstance(Figure, another_figure):
            raise ValueError
        else:
            return

